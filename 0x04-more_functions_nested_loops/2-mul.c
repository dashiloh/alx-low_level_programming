#include "main.h"
/**
 * mul - multiplies two variables
 * @a: first variable
 * @b: second variable
 *
 * Return: returns the multiplication
 */
int mul(int a, int b)
{
{
	int i;

	i = a * b;

	return (i);
}
}
